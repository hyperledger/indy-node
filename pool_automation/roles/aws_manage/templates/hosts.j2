[{{ aws_group_name }}]
{% for item in ec2.results %}
{{ aws_tag_namespace }}_{{ aws_tag_group }}{{ item.tag_id }} tag_id={{ item.tag_id }} ansible_host={{ item.public_ip }}
{% endfor %}

[{{ aws_group_name }}:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file={{ ssh_private_key }}
ansible_ssh_extra_args='-o UserKnownHostsFile={{ known_hosts_file }}'
